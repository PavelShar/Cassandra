sudo: required

language: python
python:
  - 2.7

services:
  - docker
  
  before_install:
  - docker run --name cassandra -p 22:22 -d pavelshar/cassandra:latest 
  
  script:
  - docker ps | grep -q cassandra
